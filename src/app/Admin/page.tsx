"use client";

import LoginCard from "@/components/AdminLogin//LoginCard/LoginCard";

export default function AdminLoginWrapper() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-linear-to-br from-emerald-50 via-white to-teal-100 px-4">
      <LoginCard />
    </main>
  );
}
